# Server-side base URL of the Lanyard Platform API.
# Local development usually sets API_URL directly.
API_URL=http://localhost:4000/api/v1

# Render can inject the API's internal private-network host:port instead.
# API_HOSTPORT=
# API_GLOBAL_PREFIX=api/v1
