# Server-side base URL of the Lanyard Platform API.
API_URL=http://localhost:4000/api/v1

# Render can inject the API's internal private-network host:port instead.
# API_HOSTPORT=
# API_GLOBAL_PREFIX=api/v1

# Public URLs used for SEO, cross-app links, and store handoff.
STORE_URL=http://localhost:3000
SITE_URL=http://localhost:3002

# Optional analytics.
NEXT_PUBLIC_GA_ID=API_URL=http://localhost:4000/api/v1
STORE_URL=http://localhost:3000
SITE_URL=http://localhost:3002
NEXT_PUBLIC_GA_ID=
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=no-reply@lanyard.test
CONTACT_TO=hello@lanyard.test
